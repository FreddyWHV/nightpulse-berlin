# Copy to worker/.env — never commit the real file, never ship these to the app.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Identify the crawler with a real contact address before running against venues.
BERLINBOT_UA=BerlinEventsBot/0.1 (+https://your-domain.de/bot; you@your-domain.de)

# Politeness knobs
BERLINBOT_CRAWL_DELAY=5
BERLINBOT_HOST_PARALLELISM=8

# Phase 2
ANTHROPIC_API_KEY=
KULTURDATEN_TOKEN=
TICKETMASTER_API_KEY=
